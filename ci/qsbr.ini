# Overlay for one job that builds with liburcu's QSBR flavor instead of the
# default membarrier one.  QSBR deadlocks whenever a registered thread blocks
# while it is still online, and only a full build+test run catches that.

[project options]
rcu-flavor = 'qsbr'
