.cache
.hypothesis
.mypy_cache
__pycache__
dig.out*
rndc.out*
nsupdate.out*
named.lock
named.pid
named.run
/feature-test
/test.output.*
/systests.output
/random.data
/resolve
parallel.mk

# Artifacts of the pytest runner: its log, and the temporary directories and
# convenience symlinks it creates, which have a hyphen in their names.  Tests
# run by the legacy runner may have one as well.
/pytest.conftest.log.txt
/*-*
!/*-*.c
!/*-*.sh
!/allow-query
!/filter-aaaa
!/serve-stale
